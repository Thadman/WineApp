<div>
  <%= link_to 'Home', root_path  %>
</div>

<h1> All The Listings </h1>
<%= link_to 'Add a new wine', new_listing_path %>


<div class="row justify-content-around">
  <% @listing.each do |listing| %>
  <%= render 'card', listing: listing %>
<% end %>

</div>

<%= will_paginate @listing %>
